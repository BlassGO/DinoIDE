:MAIN
   # The ARGs sent from CMD will be received by default in MAIN as parameters
   # You can try it by converting this script to .EXE and running it from CMD with additional parameters
   For COUNT ARG in MAIN
      Msg "ARG" with "ARG %COUNT% = %ARG%"