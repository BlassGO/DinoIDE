# Fibonacci(1000) using BigIntegers

n = 1000
fib = [0n 1n]   # The 'n' suffix indicates it's BigInteger
i = 2

print fib[0]
print fib[1]

while i <= n
    fib[i] = fib[i - 1] + fib[i - 2]
    print "Fib[$i] = ${fib[i]}"
    i += 1
